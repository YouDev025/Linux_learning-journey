# Basic File Commands

Review the core file and directory commands for navigating, creating, copying, moving, and deleting files in Linux.

Commands covered:
- `ls`, `cd`, `pwd`, `cp`, `mv`, `rm`
- `mkdir`, `rmdir`, `find`, `tree`
