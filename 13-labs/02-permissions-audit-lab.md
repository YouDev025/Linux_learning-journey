# Permissions Audit Lab

Objective: Audit and fix file permissions for a simple web application directory.

Tasks:
1. Inspect permissions with `ls -l`.
2. Apply least-privilege access using `chmod` and `chown`.
3. Use `stat` to verify ownership and mode.
4. Document the changes and reasoning.

Validation:
- Only owners have write access
- Group and others have minimal permissions
