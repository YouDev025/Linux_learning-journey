# Partitioning and Filesystems

Learn how to create partitions, format filesystems, and mount storage in Linux safely.

Topics:
- `fdisk`, `parted`, and `gdisk`
- Filesystem types: ext4, XFS, Btrfs
- `mkfs`, `mount`, and persistent mounts in `/etc/fstab`
