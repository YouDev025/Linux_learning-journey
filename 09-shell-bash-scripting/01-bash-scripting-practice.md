# Bash Scripting Practice

Build reusable shell scripts with control flow, input handling, and basic error checking.

Topics:
- `if`, `case`, and loops
- Input validation and exit codes
- Debugging scripts with `set -e` and `set -x`
