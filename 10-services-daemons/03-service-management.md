# Service Management

Practice enabling, starting, stopping, and checking the status of services in systemd-based Linux systems.

Topics:
- `systemctl start`, `stop`, `restart`
- `systemctl enable` and `disable`
- Diagnosing failed services
