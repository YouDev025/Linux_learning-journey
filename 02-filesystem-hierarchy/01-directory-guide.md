# Directory Guide

A directory-by-directory guide to the Linux filesystem. Learn where configuration files, binaries, logs, device nodes, and user data belong.

Topics:
- `/etc` for configuration
- `/var` for variable data
- `/usr` for shared programs
- `/tmp`, `/boot`, `/opt`, `/root`
