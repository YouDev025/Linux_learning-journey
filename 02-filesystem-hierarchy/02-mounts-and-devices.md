# Mounts and Device Files

Learn how mounts work in Linux, the difference between physical disks and mounted filesystems, and how `/dev` represents devices.

Topics:
- `mount` and `umount`
- Filesystem table `/etc/fstab`
- Virtual filesystems: `proc`, `sysfs`, `tmpfs`
