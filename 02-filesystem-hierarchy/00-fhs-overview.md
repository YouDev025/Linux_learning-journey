# Filesystem Hierarchy Standard (FHS)

Understand the Linux directory layout and the purpose of each top-level directory defined by the Filesystem Hierarchy Standard.

Key directories:
- `/`: root filesystem
- `/bin`, `/sbin`, `/usr`, `/var`, `/etc`, `/home`
- `/proc`, `/sys`, `/dev`
