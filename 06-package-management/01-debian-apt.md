# Debian Package Management with APT

Use APT to install, update, and manage packages on Debian-based distributions, and learn how to inspect package sources.

Topics:
- `apt update`, `apt upgrade`, `apt install`, `apt remove`
- Repository configuration in `/etc/apt/sources.list`
- Pinning, caching, and package audits
